//! No invariants were authored for the `{{ unit }}` component.
//!
//! The author stopped rather than publish a suite that would only look checked. Selecting this
//! feature therefore fails the build, on purpose — there is nothing behind it to fuzz, and a harness
//! that reported anything here would be reporting something nobody verified.

compile_error!("{{ feature }}: no invariants were authored for the {{ unit }} component — {{ reason }}");
