//! Authored invariants for the `{{ feature }}` component.
//!
//! Reached from the crate root's generated `#[invariant_test] fn {{ feature }}`, which this
//! module's `{{ section_fn }}` is called from once after every fuzzed action.

use super::*;

{{ body }}
