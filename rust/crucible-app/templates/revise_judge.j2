

Your previous suite COMPILED but a security reviewer REJECTED it — this is NOT a build error. Revise the tests to
address the review feedback below (each point names a criterion and a concrete fix):
{{ feedback }}

Prior source:
```rust
{{ prev_src }}
```
