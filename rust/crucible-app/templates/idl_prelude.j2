// The program's Anchor types, generated from its IDL — the harness does NOT depend on the program's
// crate, so its Solana/Anchor toolchain never has to match this harness's (docs/idl-gen.md).
crucible_idl_gen::declare_fuzz_program!({{ module }} = "{{ path }}");
